# Copy to .env.local and adjust. Never commit the real file.

# Base URL of the NASA-WEB-APP API. Read on the server only, so it is not
# exposed to the browser and must not carry the NEXT_PUBLIC_ prefix.
API_URL=http://localhost:3000
